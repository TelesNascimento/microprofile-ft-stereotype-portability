# Screenshots

Drop the specification screenshots here using the exact file names below. The main README
references these paths, so the images render automatically once the files are present.

| File name | Capture from | What to show |
|---|---|---|
| `p1-ft-spec-interceptor-bindings.png` | MicroProfile Fault Tolerance 4.1.1 spec, "Relationship to other specifications" | The sentence stating the annotations are interceptor bindings and the spec depends on Jakarta Interceptors and CDI |
| `p3-ft-spec-class-level.png` | Same MicroProfile Fault Tolerance section | The sentence stating a class-level annotation applies to all business methods of the class |
| `p4-cdi-8-1-1-stereotype-inherited.png` | Jakarta CDI 4.1 spec, section 8.1.1 | The sentence stating a stereotype-declared interceptor binding is inherited by any bean that declares the stereotype, and the `@Target(TYPE)` requirement |
| `p5-cdi-8-3-class-replaces-stereotype.png` | Jakarta CDI 4.1 spec, section 8.3 | The sentence stating a class-level interceptor binding replaces the one declared by a stereotype |

Source URLs are listed in the main README under Sources.
